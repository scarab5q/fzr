extern crate fst;
extern crate fst_levenshtein;
extern crate fst_regex;
extern crate futures_io;

use futures_io::iovec::IoVec;
use std::env;

fn main() {
    let args: Vec<_> = env::args().collect();

    let mut data = vec![];
}
